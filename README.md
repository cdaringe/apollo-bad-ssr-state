# apollo-naughty-ssr-demo

## demo

- `npm ci`
- `npm run start`
- attach a debugger
- `curl localhost:3000`

follow the rich, detailed comments along the `debugger` breakpoints,
mainly in `src/middlewares/ssr.ts`

